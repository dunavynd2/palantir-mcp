## Use secure secret storage in production and restrict file permissions (for example chmod 600).
## Never commit real token values to source control.
FOUNDRY_API_URL=https://<enrollment>.palantirfoundry.com
FOUNDRY_TOKEN=<token>
